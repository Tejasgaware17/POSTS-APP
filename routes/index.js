const authRoutes = require("./routes.auth");
const exploreRoutes = require("./routes.explore");
const postsRoutes = require("./routes.posts");

module.exports = { authRoutes, exploreRoutes, postsRoutes };
